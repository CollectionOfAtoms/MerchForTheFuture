A labeled single-line text field for forms (contact form, listing fields, checkout address).

```jsx
<Input label="Email" type="email" placeholder="you@example.com" />
```

Border goes strawberry-red and shows an error string underneath when `error` is set. Square-ish corners (`--radius-sm`) unlike the fully-pill buttons — inputs are the one exception to the pill rule.
