The `/shop` browse-grid tile: square product image, title, price, and an "Available in N colors" hint. No hover overlay (unlike ArtworkCard) — apparel tiles link straight to the product page.

```jsx
<ApparelCard image={photo} title="Solar Punk Bee Tee" price="$32" colorCount={3} />
```
