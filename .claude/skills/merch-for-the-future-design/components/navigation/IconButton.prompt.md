A round dark-glass chevron button for image carousels and the Discover bento pop-out cards. The chevron is a hand-drawn stroke `<polyline>` — the brand never uses an icon font or icon library.

```jsx
<IconButton direction="left" onClick={prevImage} />
```
